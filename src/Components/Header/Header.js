import "./Header.css";

const Header = () => {
    return (
        <div className="Header">
            <h1 className="Header__title">do|doing|done</h1>
            <nav></nav>
        </div>
    );
};

export default Header;
